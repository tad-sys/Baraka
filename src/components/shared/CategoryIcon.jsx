import React from 'react';import { TreePine, UtensilsCrossed, Droplets, Home, Moon, GraduationCap, Heart, Gift, Sparkles, HelpCircle } from 'lucide-react';const icons = {  arbre: TreePine,  repas: UtensilsCrossed,  puits: Droplets,  maison: Home,  mosquee: Moon,  ecole: GraduationCap,  orphelin: Heart,  mariage: Sparkles,  cadeau: Gift,  autre: HelpCircle,};const labels = {  arbre: 'Plantation d\'arbre',  repas: 'Distribution de repas',  puits: 'Construction de puits',  maison: 'Construction de maison',  mosquee: 'Construction de mosquée',  ecole: 'Construction d\'école',  orphelin: 'Parrainage orphelin',  mariage: 'Mariage',  cadeau: 'Cadeau',  autre: 'Autre',};export function getCategoryIcon(category) {  return icons[category] || HelpCircle;}export function getCategoryLabel(category) {  return labels[category] || 'Autre';}export default function CategoryIcon({ category, size = 'md', className = '' }) {  const Icon = getCategoryIcon(category);  const sizeClass = size === 'sm' ? 'w-4 h-4' : size === 'lg' ? 'w-7 h-7' : 'w-5 h-5';  return <Icon className={`${sizeClass} ${className}`} />;}