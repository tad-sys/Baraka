import React from 'react';import { motion } from 'framer-motion';import { ListPlus, Share2, TrendingUp } from 'lucide-react';const steps = [  {    icon: ListPlus,    title: 'Créez votre liste',    description: 'Ajoutez vos actions solidaires avec des liens de paiement vers les associations de votre choix.',  },  {    icon: Share2,    title: 'Partagez-la',    description: 'Envoyez votre liste à vos proches pour un mariage, une naissance, ou tout autre événement.',  },  {    icon: TrendingUp,    title: 'Suivez la progression',    description: 'Visualisez en temps réel les participations et l\'avancement de chaque action.',  },];export default function HowItWorks() {  return (    <section className="py-20 bg-gradient-to-b from-background to-secondary/30">      <div className="max-w-5xl mx-auto px-4 sm:px-6">        <div className="text-center mb-14">          <h2 className="font-display text-3xl md:text-4xl font-bold text-foreground mb-3">            Comment ça marche ?          </h2>          <p className="text-muted-foreground">En 3 étapes simples</p>        </div>        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">          {steps.map((step, index) => (            <motion.div              key={step.title}              initial={{ opacity: 0, y: 20 }}              whileInView={{ opacity: 1, y: 0 }}              viewport={{ once: true }}              transition={{ duration: 0.5, delay: index * 0.15 }}              className="text-center"            >              <div className="w-16 h-16 rounded-2xl bg-primary/10 flex items-center justify-center mx-auto mb-5">                <step.icon className="w-7 h-7 text-primary" />              </div>              <div className="text-xs font-bold text-primary/60 uppercase tracking-widest mb-2">                Étape {index + 1}              </div>              <h3 className="text-xl font-semibold text-foreground mb-2">{step.title}</h3>              <p className="text-muted-foreground text-sm leading-relaxed">{step.description}</p>            </motion.div>          ))}        </div>      </div>    </section>  );}