import React from 'react';import { motion } from 'framer-motion';import { Card } from '@/components/ui/card';import { Button } from '@/components/ui/button';import { ExternalLink } from 'lucide-react';import CategoryIcon, { getCategoryLabel } from '../shared/CategoryIcon';import ProgressRing from '../shared/ProgressRing';export default function FeaturedActionCard({ action, index = 0 }) {  const progress = action.goal_quantity > 0     ? Math.min((action.current_quantity / action.goal_quantity) * 100, 100)     : 0;  return (    <motion.div      initial={{ opacity: 0, y: 30 }}      animate={{ opacity: 1, y: 0 }}      transition={{ duration: 0.5, delay: index * 0.1 }}    >      <Card className="group overflow-hidden border-0 shadow-md hover:shadow-xl transition-all duration-500 bg-card">        {/* Image */}        <div className="relative h-48 overflow-hidden bg-gradient-to-br from-primary/10 to-accent/20">          {action.image_url ? (            <img              src={action.image_url}              alt={action.title}              className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"            />          ) : (            <div className="w-full h-full flex items-center justify-center">              <CategoryIcon category={action.category} size="lg" className="text-primary/40 w-16 h-16" />            </div>          )}          <div className="absolute top-3 left-3">            <span className="inline-flex items-center gap-1.5 bg-card/90 backdrop-blur-sm text-foreground text-xs font-medium px-3 py-1.5 rounded-full">              <CategoryIcon category={action.category} size="sm" />              {getCategoryLabel(action.category)}            </span>          </div>        </div>        {/* Content */}        <div className="p-5">          <h3 className="font-semibold text-lg text-foreground mb-1 line-clamp-1">            {action.title}          </h3>          {action.association_name && (            <p className="text-xs text-muted-foreground mb-3">              via {action.association_name}            </p>          )}          {action.description && (            <p className="text-sm text-muted-foreground line-clamp-2 mb-4">              {action.description}            </p>          )}          {/* Progress */}          <div className="flex items-center justify-between mb-4">            <div>              <p className="text-2xl font-bold text-foreground">                {action.current_quantity || 0}                <span className="text-sm font-normal text-muted-foreground"> / {action.goal_quantity}</span>              </p>              <p className="text-xs text-muted-foreground">                {action.unit_price}€ par unité              </p>            </div>            <ProgressRing              current={action.current_quantity || 0}              goal={action.goal_quantity}              size={56}              strokeWidth={5}            />          </div>          {/* Progress bar */}          <div className="w-full bg-muted rounded-full h-2 mb-4 overflow-hidden">            <motion.div              initial={{ width: 0 }}              animate={{ width: `${progress}%` }}              transition={{ duration: 1, delay: 0.3 }}              className="h-full bg-primary rounded-full"            />          </div>          {/* Action button */}          {action.payment_link && (            <a href={action.payment_link} target="_blank" rel="noopener noreferrer">              <Button className="w-full rounded-full" size="sm">                Participer                <ExternalLink className="ml-2 w-4 h-4" />              </Button>            </a>          )}        </div>      </Card>    </motion.div>  );}