import React from 'react';import { Sparkles } from 'lucide-react';import FeaturedActionCard from './FeaturedActionCard';export default function FeaturedSection({ actions, isLoading }) {  if (isLoading) {    return (      <section className="py-16 px-4 sm:px-6 max-w-6xl mx-auto">        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">          {[1, 2, 3].map((i) => (            <div key={i} className="bg-card rounded-xl h-96 animate-pulse" />          ))}        </div>      </section>    );  }  if (!actions || actions.length === 0) return null;  return (    <section className="py-16 px-4 sm:px-6 max-w-6xl mx-auto">      <div className="text-center mb-12">        <div className="inline-flex items-center gap-2 text-primary mb-4">          <Sparkles className="w-5 h-5" />          <span className="text-sm font-semibold uppercase tracking-wider">En vedette</span>        </div>        <h2 className="font-display text-3xl md:text-4xl font-bold text-foreground mb-3">          Actions mises en avant        </h2>        <p className="text-muted-foreground max-w-xl mx-auto">          Découvrez les actions solidaires qui comptent et participez à votre mesure.        </p>      </div>      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">        {actions.map((action, index) => (          <FeaturedActionCard key={action.id} action={action} index={index} />        ))}      </div>    </section>  );}